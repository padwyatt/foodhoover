CREATE TABLE public.agg_district_run
(
    postcode_district text COLLATE pg_catalog."default" NOT NULL,
    run_id text COLLATE pg_catalog."default" NOT NULL,
    scrape_time timestamp without time zone,
    roo integer,
    je integer,
    ue integer,
    fh integer,
    CONSTRAINT agg_district_run_pk PRIMARY KEY (postcode_district, run_id)
);